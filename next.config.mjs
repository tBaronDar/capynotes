/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"avatars.githubusercontent.com",
			"media.licdn.com",
			"themis-generic-images.s3.eu-north-1.amazonaws.com",
		],
	},
};

export default nextConfig;
